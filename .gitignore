# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Built binaries
stackaroo
/bin/
/dist/

# All dotfiles and dotdirectories (IDE, OS, config files)
.*

# But allow specific dotfiles we want to track
!.github
!.gitignore
!.goreleaser.yml

# Temporary files
tmp/
*.tmp
*.temp

# Log files
*.log

# Local stackaroo state/cache (already covered by .* above)

# Test coverage
coverage.out
coverage.html

# Release artifacts
releases/
